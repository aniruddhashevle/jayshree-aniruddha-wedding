$(document).ready(function () {
  $(".slider").slick({
    dots: true,
    // infinite: true,
    speed: 500,
    // fade: true,
    cssEase: "linear",
  });
  // setTimeout(function () {
  //   $(window).trigger("scroll");
  //   AOS.init();
  // }, 200);
});
